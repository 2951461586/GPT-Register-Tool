# Historical Audits

Files named `audit-*.md`, `scan-*.md`, and `*-assessment-*.md` are historical
evidence snapshots. They may describe superseded code and must not be treated as
the current architecture contract. Current ownership and rules live in
`docs/architecture.md`, `docs/directory-map.md`, and `docs/CONTEXT.md`.
