# v2026.09.06.1

## CI 修复

- 修复浏览器流程 patch 守卫测试使用固定 `F:` 路径，改为从测试文件定位仓库根目录，兼容 GitHub Actions 工作目录。
- 修复路径契约测试硬编码 `F:` 盘符，改为使用当前仓库盘符。
- 修复配置使用检测测试依赖本机未跟踪 `config.json` 的问题。CI 使用干净的 `config.example.json` 时允许零个未读配置项；本地保留历史未读项时仍继续校验固定集合。
- 修复空配置报告测试，使其同时覆盖“无未读项”和“存在未读项”两种合法状态。

## 验证

- Python：全量测试通过。
- .NET：全量测试通过。
- 发布包重新执行 payload、敏感字段和文档一致性扫描。
